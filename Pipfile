[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
setuptools = ">=49.6.0"
wheel = ">=0.35.1"
twine = ">=3.2.0"
python-decouple = ">=3.3"
pytest = ">=6.1.2"
testfixtures = ">=6.15.0"

[packages]
requests = ">=2.25.1"
pandas = ">=1.2.1"

[requires]
python_version = "3.8"
